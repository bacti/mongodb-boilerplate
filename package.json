{
  "name": "rnd",
  "author": "",
  "license": "ISC",
  "version": "0.0.1",
  "scripts": {
    "dev": "%NODE_PATH%/.bin/cross-env NODE_ENV=development %NODE_PATH%/.bin/webpack-dev-server -d --devtool source-map --config index.js --port 8080 --host 0.0.0.0",
    "release": "%NODE_PATH%/.bin/cross-env NODE_ENV=production %NODE_PATH%/.bin/webpack --config index.js --progress --profile --colors",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "mongodb": "^3.0.7",
    "qrcode": "^1.2.0"
  }
}
